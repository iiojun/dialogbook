<nav aria-label="breadcrumb">
  <ol class="breadcrumb mb-3 bg-white">
    <% items.each do |label, path| %>
      <li class="breadcrumb-item">
        <%= path ? link_to(label, path) : label %>
      </li>
    <% end %>
  </ol>
</nav>
