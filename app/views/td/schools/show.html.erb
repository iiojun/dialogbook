<%= render "shared/breadcrumbs",
      items: [["My Page", root_path], ["Class Comments", nil]] %>
<h1 class="mb-3">Class Comments</h1>
<% if @posts&.length == 0 %>
  No comments posted in this class.
<% else %>
  <%= render @posts %>
  <%= paginate @posts %>
<% end %>
