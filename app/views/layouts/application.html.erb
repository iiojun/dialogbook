<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Dialogbook3" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/sandstone/bootstrap.min.css", media: "all", "data-turbo-track": "reload" %>

  </head>

  <body>
    <header>
      <nav class="navbar navbar-dark bg-dark">
        <% if user_signed_in? %>
          <div class="btn btn-success ms-2">
            <% if current_user.name != nil %>
              <%= link_to "MyPage", mypage_user_path(current_user),
                  class: "text-white" %>
            <% else %>
              <span class: "text-white">MyPage</span>
            <% end %>
          </div>
          <div class="text-white">
            <%= current_user.name %>
          </div>
          <div class="me-2">
            <%= button_to "Sign out", sign_out_path, method: :get,
                  data: { turbo: false }, class: "btn btn-danger" %>
          </div>
        <% else %>
          <div class="ms-2">
            <%= button_to "Sign in", "/auth/auth0", method: :post,
                 data: { turbo: false }, class: "btn btn-primary" %></div>
          <div class="text-white">Dialogbook</div>
          <div class="btn btn-info me-2">
            <%= link_to "info", "https://www.kotoba-kobo.jp",
                class: "text-white" %>
          </div>
        <% end %>
      </nav>
    </header>

    <% if notice %>
      <div class="alert alert-info alert-dismissible fade show" role="alert">
        <%= notice %>
        <button type="button" class="btn-close"
                data-bs-dismiss="alert" aria-label="dismiss">
        </button>
      </div>
    <% end %>
    <% if alert %>
      <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <%= alert %>
        <button type="button" class="btn-close"
                data-bs-dismiss="alert" aria-label="dismiss">
        </button>
      </div>
    <% end %>
    <div class="container mt-3">
      <%= yield %>
    </div>
  </body>
</html>
